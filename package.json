{
  "name": "leos-dough",
  "version": "1.0.0",
  "private": true,
  "description": "Leo's Dough — one-page Jekyll menu",
  "scripts": {
    "dev": "concurrently \"npm run css:watch\" \"bundle exec jekyll serve\"",
    "css:watch": "tailwindcss -i ./src/input.css -o ./assets/css/style.css --watch",
    "css:build": "tailwindcss -i ./src/input.css -o ./assets/css/style.css --minify",
    "build": "npm run css:build && bundle exec jekyll build",
    "deploy": "npm run css:build && bundle exec jekyll build --baseurl /dough --destination _gh-pages && touch _gh-pages/.nojekyll && gh-pages -d _gh-pages --dotfiles"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "concurrently": "^8.2.2",
    "gh-pages": "^6.1.1",
    "postcss": "^8.4.33",
    "qrcode": "^1.5.4",
    "tailwindcss": "^3.4.1"
  }
}
